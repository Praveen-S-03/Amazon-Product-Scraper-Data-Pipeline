# Amazon-Product-Scraper-Data-Pipeline
A Python-based web scraping project that collects product data from Amazon, stores the data in JSON format, and loads it into a PostgreSQL database.   This project demonstrates a complete **data pipeline using Selenium, JSON, and PostgreSQL with logging and error handling**.
